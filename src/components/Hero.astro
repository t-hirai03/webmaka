---

---

<section id="hero" class="hero">
  <div class="hero__container">
    <h1 class="hero__title">
      Webサイト制作を、<br class="hero__br-sp" />ワンストップで。
    </h1>
    <p class="hero__subtitle">
      相談から納品まで、窓口ひとつで対応します
    </p>
  </div>
</section>

<style>
  .hero {
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--color-bg-main);
  }

  .hero__container {
    max-width: var(--container-max);
    margin-inline: auto;
    padding-inline: 1rem;
    text-align: center;
  }

  .hero__title {
    font-size: 1.875rem;
    font-weight: 700;
    color: var(--color-text);
    margin-block-end: 1.5rem;
  }

  @media (min-width: 768px) {
    .hero__title {
      font-size: 3rem;
    }

    .hero__br-sp {
      display: none;
    }
  }

  .hero__subtitle {
    font-size: 1.125rem;
    color: var(--color-text-muted);
  }

  @media (min-width: 768px) {
    .hero__subtitle {
      font-size: 1.25rem;
    }
  }
</style>
