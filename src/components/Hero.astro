---

---

<section id="hero" class="pt-10 md:py-10 flex items-center justify-center bg-bg-main">
  <div class="max-w-[var(--container-max)] mx-auto px-4 text-center">
    <h1 class="text-3xl md:text-5xl font-bold text-text mb-6">
      <span class="hero__marker">Webサイト・LP制作、<br class="md:hidden" />既存サイトのCMS化対応いたします。</span>
    </h1>
  </div>
</section>

<style>
  .hero__marker {
    background: linear-gradient(transparent 60%, rgba(61, 139, 110, 0.3) 60%);
    background-size: 0% 100%;
    background-repeat: no-repeat;
    animation: marker-draw 1s ease-out 0.3s forwards;
  }

  @keyframes marker-draw {
    to {
      background-size: 100% 100%;
    }
  }
</style>
