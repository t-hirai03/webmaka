---
import SectionTitle from './ui/SectionTitle.astro';
import { Mail, MessageCircle, PenTool, Rocket } from 'lucide-astro';
---

<section id="flow" class="flow">
  <div class="flow__container">
    <SectionTitle title="Flow" subtitle="ご依頼の流れ" />
    <ol class="flow__list">
      <li class="flow-card">
        <span class="flow-card__number">1</span>
        <div class="flow-card__icon">
          <Mail size={28} strokeWidth={1.5} />
        </div>
        <h3 class="flow-card__title">お問い合わせ</h3>
        <p class="flow-card__description">フォームからご連絡ください</p>
      </li>
      <li class="flow-card">
        <span class="flow-card__number">2</span>
        <div class="flow-card__icon">
          <MessageCircle size={28} strokeWidth={1.5} />
        </div>
        <h3 class="flow-card__title">ヒアリング・お見積もり</h3>
        <p class="flow-card__description">ご要望を伺い、お見積もりを提示します</p>
      </li>
      <li class="flow-card">
        <span class="flow-card__number">3</span>
        <div class="flow-card__icon">
          <PenTool size={28} strokeWidth={1.5} />
        </div>
        <h3 class="flow-card__title">制作・確認</h3>
        <p class="flow-card__description">制作を進めながら確認・修正を行います</p>
      </li>
      <li class="flow-card">
        <span class="flow-card__number">4</span>
        <div class="flow-card__icon">
          <Rocket size={28} strokeWidth={1.5} />
        </div>
        <h3 class="flow-card__title">納品・公開</h3>
        <p class="flow-card__description">完成後、納品・公開対応をします</p>
      </li>
    </ol>
  </div>
</section>

<style>
  .flow {
    padding-block: 5rem 6rem;
    background-color: var(--color-bg-main);
  }

  @media (min-width: 768px) {
    .flow {
      padding-block: 7rem 8rem;
    }
  }

  .flow__container {
    max-width: var(--container-max);
    margin-inline: auto;
    padding-inline: 1rem;
  }

  .flow__list {
    display: grid;
    gap: 1.5rem;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  @media (min-width: 768px) {
    .flow__list {
      grid-template-columns: repeat(4, 1fr);
      gap: 1rem;
    }
  }

  .flow-card {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    background-color: #fff;
    border-radius: 1rem;
    padding: 2rem 1.5rem;
    box-shadow: 0 1px 3px rgb(0 0 0 / 0.06);
  }

  .flow-card__number {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    font-size: 0.875rem;
    font-weight: 700;
    color: #fff;
    background-color: var(--color-accent);
    border-radius: 50%;
    margin-block-end: 1rem;
  }

  .flow-card__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 4rem;
    height: 4rem;
    background: linear-gradient(135deg, var(--color-primary-light) 0%, var(--color-primary) 100%);
    border-radius: 1rem;
    margin-block-end: 1.25rem;
  }

  .flow-card__icon :global(svg) {
    stroke: #fff;
  }

  .flow-card__title {
    font-size: 1.125rem;
    font-weight: 700;
    color: var(--color-text);
    margin: 0;
    margin-block-end: 0.5rem;
  }

  .flow-card__description {
    font-size: 0.875rem;
    line-height: 1.6;
    color: var(--color-text-muted);
    margin: 0;
  }
</style>
