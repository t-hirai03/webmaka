---
import { FileQuestion } from 'lucide-astro';
import Button from '@/components/ui/Button.astro';
import Layout from '@/layouts/Layout.astro';
---

<Layout title="ページが見つかりません | うぇぶまか" noindex>
  <section class="pt-[calc(48px+var(--height-header-mobile))] pb-16 sm:py-12 sm:pb-16 md:py-28 md:pb-32 bg-bg-section min-h-page flex items-center">
    <div class="max-w-container mx-auto px-4 w-full">
      <div class="max-w-xl mx-auto text-center bg-white py-12 px-8 rounded-xl shadow-sm">
        <div class="text-text-muted mb-4 flex justify-center">
          <FileQuestion size={64} strokeWidth={1.5} />
        </div>
        <h1 class="text-6xl md:text-8xl font-bold text-accent leading-none mb-2">404</h1>
        <p class="text-xl md:text-2xl font-semibold text-text mb-6">ページが見つかりません</p>
        <p class="text-text-muted leading-relaxed-plus mb-8">
          お探しのページは移動または削除された可能性があります。<br />
          URLをご確認いただくか、トップページからお探しください。
        </p>
        <div class="flex justify-center">
          <Button href="/" variant="secondary" size="lg">
            トップページに戻る
          </Button>
        </div>
      </div>
    </div>
  </section>
</Layout>
